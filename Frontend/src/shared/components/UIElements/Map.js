import React from 'react'

import './Map.css';

const Map = () => {
    return (
        <div className={`map ${this.props.className}`} style={props.style}>
            
        </div>
    )
}

export default Map
